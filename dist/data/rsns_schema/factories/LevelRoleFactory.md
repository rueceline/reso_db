# LevelRoleFactory

## Schema

```js
LevelRoleFactory = {
  id,
  idCN,
  mod,
  isInformalData,
  awakeLv,
  cardNum1,
  cardNum2,
  cardNum3,
  e1s1Id, // -> SkillFactory.id
  e1s1NumSN,
  e1s2Id, // -> SkillFactory.id
  e1s2NumSN,
  e1s3Id, // -> SkillFactory.id
  e1s3NumSN,
  e1s4Id, // -> SkillFactory.id
  e1s4NumSN,
  e1s5Id,
  e1s5NumSN,
  e1s6Id,
  e1s6NumSN,
  e2s1Id, // -> SkillFactory.id
  e2s1NumSN,
  e2s2Id, // -> SkillFactory.id
  e2s2NumSN,
  e2s3Id, // -> SkillFactory.id
  e2s3NumSN,
  e2s4Id, // -> SkillFactory.id
  e2s4NumSN,
  e2s5Id,
  e2s5NumSN,
  e2s6Id,
  e2s6NumSN,
  e3s1Id, // -> SkillFactory.id
  e3s1NumSN,
  e3s2Id, // -> SkillFactory.id
  e3s2NumSN,
  e3s3Id, // -> SkillFactory.id
  e3s3NumSN,
  e3s4Id, // -> SkillFactory.id
  e3s4NumSN,
  e3s5Id, // -> SkillFactory.id
  e3s5NumSN,
  e3s6Id, // -> SkillFactory.id
  e3s6NumSN,
  equip1Id, // -> EquipmentFactory.id
  equip1Lv,
  equip2Id, // -> EquipmentFactory.id
  equip2Lv,
  equip3Id, // -> EquipmentFactory.id
  equip3Lv,
  lv,
  resonanceLv,
  skill1Lv,
  skill2Lv,
  skill3Lv,
  unitId, // -> UnitFactory.id
  unitViewId, // -> UnitViewFactory.id
  usePlayerRoleData,
}
```
