# BattleInfoFactory

## Schema

```js
BattleInfoFactory = {
  id,
  idCN,
  mod,
  isInformalData,
  baseImgPath,
  baseMatPath,
  bossTipPath,
  duration: [any],
  fadeInDistance,
  fadeInFrame,
  fadeOutDistance,
  fadeOutFrame,
  followEffect,
  interval,
  keepFrame,
  moveDistanceY,
  moveDuration: [any],
  moveStartTime: [any],
  num0Id,
  num1Id,
  num2Id,
  num3Id,
  num4Id,
  num5Id,
  num6Id,
  num7Id,
  num8Id,
  num9Id,
  offsetXMax,
  offsetXMin,
  offsetYMax,
  offsetYMin,
  prefab,
  resStr,
  scale: [any],
  scaleDuration: [any],
  scaleStart,
  scaleStartTime: [any],
  shieldImgPath,
  shieldMatPath,
  subHPList: [
    {
      imgPath,
      matPath,
    }
  ],
  tagId,
  textId,
  tweenImgPath,
  tweenMatPath,
}
```
