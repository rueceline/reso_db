// =========================
// 1. 상수 / 매핑
// =========================



// =========================
// 2. 기본 유틸
// =========================

export function normalizePathSlash(s) {
  return String(s || '').replaceAll('\\', '/');
}
