# MapSessionFactory

## Schema

```js
MapSessionFactory = {
  id,
  idCN,
  mod,
  isInformalData,
  cameraData,
  cameraPath,
  durationFrame,
  mapNeedleList: [
    {
      id,
    }
  ],
  needWinlevel,
  onEnterArgs,
  onEnterId,
  onInitArgs,
  onInitId,
  onLeaveActon,
  onLeaveArgs,
  onLeaveId,
  onLevelWinOpenSession,
  rotationMaxX,
  rotationMaxY,
  rotationMinX,
  rotationMinY,
  singleOnEnter,
  singleOnInit,
}
```
